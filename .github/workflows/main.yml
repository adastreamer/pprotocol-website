name: DEPLOY

on:
  push:
    branches:
    - master

jobs:
  deploy:

    runs-on: ubuntu-latest

    steps:

    - name: deploy the latest code
      run: echo '${{ secrets.DEPLOYER_KEY }}' > deploy.key && cat deploy.key
